package emils
a[b]		//a.getAt(b)
a[b] = c	//a.putAt(b, c)
a << b		//a.leftShift(b)
a >> b		//a.rightShift(b)
~a			//a.bitwiseNegate()
-a			//a.negative()
+a			//a.positive()
a == b		//a.equals(b) or a.compareTo(b) == 0
a != b		//! a.equals(b)
a <=> b		//a.compareTo(b)
a > b		//a.compareTo(b) > 0
a >= b		//a.compareTo(b) >= 0
a < b		//a.compareTo(b) < 0
a <= b		//a.compareTo(b) <= 0 