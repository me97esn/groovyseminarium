package se.britech.groovySeminarie.groovy
a[b] 		//a.getAt(b)
a[b] = c 	//a.putAt(b, c)
a <<     	//a.leftShift(b)
a >>     	//a.rightShift(b)
~a 			//a.bitwiseNegate()
-a 			//a.negative()
+a 			//a.positive()
a ==     	//a.equals(b) or a.compareTo(b) == 0
a !=     	//! a.equals(b)
a <=>     	//a.compareTo(b)
a >     	//a.compareTo(b) > 0
a >=     	//a.compareTo(b) >= 0
a <     	//a.compareTo(b) < 0
a <=     	//a.compareTo(b) <= 0 